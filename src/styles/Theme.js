import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main:"#000080",
    },
    secondary: {
      main: "#FFFDD0",
    },
  },
});


export default theme
