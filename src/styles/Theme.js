import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000080",
    },
    secondary: {
      main: "#FFFDD0",
    },
    gray: {
      main: "#424242",
    },
  },
});


export default theme
